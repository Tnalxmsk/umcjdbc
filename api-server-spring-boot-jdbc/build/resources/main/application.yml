server:
  port: 8080

spring:
  application:
    name: demo
  profiles:
    active: local

  sql:
    init:
      platform: mysql

  datasource:
    url: jdbc:mysql://localhost:8080/ #3306/springboot?autoReconnect=true&setTimezone=Asia/Seoul # 변경해주세요
    #username: test # Database ID
    #password: test1234^^ # Database Password
    driver-class-name: com.mysql.cj.jdbc.Driver